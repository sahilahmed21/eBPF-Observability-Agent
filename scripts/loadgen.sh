#!/usr/bin/env bash
# Fixed synthetic load for overhead comparability across phases.
# Flesh out once demos/http-server exists (wrk/hey against a pinned URL).
set -euo pipefail

echo "loadgen: placeholder — wire to demos/http-server in Phase 1."
echo "Intent: fixed RPS/concurrency every measurement run."
exit 0
