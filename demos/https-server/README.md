# HTTPS demo server (Phase 3)

Planned: TLS-terminated companion (self-signed cert OK) linked against OpenSSL
so `SSL_read` / `SSL_write` uprobes resolve.

Document which `libssl` soname the binary links against in this README once built.
