# `benches/`

Fixed load scripts (`wrk` / custom) and notes for overhead measurement. Same script every phase → `docs/overhead.md`.

Vision-95 headline load (Phase 11): [vision95-load.md](vision95-load.md).
