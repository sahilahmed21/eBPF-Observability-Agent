# Multi-stage Dockerfile (Phase 4)

Planned stages:

1. Build eBPF bytecode + userspace agent (musl static where practical).
2. Runtime: `distroless` or `scratch` with the agent binary only.

Requires a host/node kernel with BTF; the container does not ship a kernel.
