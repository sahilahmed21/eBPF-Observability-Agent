# Phase 4 — agent configuration (sampling, redaction toggles, OTLP endpoint).
apiVersion: v1
kind: ConfigMap
metadata:
  name: ebpf-obs-agent
  namespace: observability
data:
  # otlp_endpoint: "otel-collector.observability:4317"
  # ringbuf_drop_log: "true"
  config.yaml: |
    # placeholder
