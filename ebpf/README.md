# `ebpf/`

Aya eBPF programs (kprobes, tracepoints, uprobes). Compiled to BPF bytecode; loaded by `agent/`.

**Empty until Phase 0** — scaffold with `aya-template` / `cargo generate`, then move crates here to match the workspace layout in the root README.
