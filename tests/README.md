# Integration / correctness harnesses

| Path | Purpose |
|------|---------|
| `correctness/` | Assert agent p50/p99 vs injectable server latency |
| `fixtures/` | Sample HTTP prefix byte captures for parser unit tests |

Populated starting Phase 1–2.
